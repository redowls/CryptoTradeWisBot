"""The ONLY module that reads/writes app_config (credentials + params). Phase 15 will encrypt secrets here without touching the rest of the code.

Stub — implemented in Phase 1. See todo.md / summary.md.
"""
